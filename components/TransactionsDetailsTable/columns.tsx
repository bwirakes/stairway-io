// components/TransactionDetailsTable/columns.tsx
import { ColumnDef } from '@tanstack/react-table';
import { Transaction } from '@/components/types';

export const columns: ColumnDef<Transaction>[] = [
  // ... (same as earlier)
];
